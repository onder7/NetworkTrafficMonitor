<Window x:Class="NetworkTrafficMonitor.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About - Network Traffic Monitor" 
        Height="500" Width="600"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Segoe UI">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Logo/Icon -->
        <TextBlock Grid.Row="0" 
                   Text="🌐" 
                   FontSize="64" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- App Name -->
        <TextBlock Grid.Row="1" 
                   Text="Network Traffic Monitor" 
                   FontSize="28" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Foreground="{StaticResource AccentBrush}"/>

        <!-- Version -->
        <TextBlock Grid.Row="2" 
                   Text="Version 5.0 - Complete Edition" 
                   FontSize="14" 
                   HorizontalAlignment="Center"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="0,5,0,30"/>

        <!-- Info Panel -->
        <Border Grid.Row="3" 
                Background="{StaticResource PanelBrush}"
                CornerRadius="8"
                Padding="20">
            <StackPanel>
                <!-- Developer -->
                <TextBlock Text="👨‍💻 Geliştirici" 
                           FontSize="16" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>
                <TextBlock Text="Önder Aköz" 
                           FontSize="14"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,0,20"/>

                <!-- GitHub -->
                <TextBlock Text="🔗 GitHub" 
                           FontSize="16" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>
                <TextBlock FontSize="14" Margin="0,0,0,20">
                    <Hyperlink NavigateUri="https://github.com/onder7" 
                               RequestNavigate="Hyperlink_RequestNavigate"
                               Foreground="{StaticResource AccentBrush}">
                        github.com/onder7
                    </Hyperlink>
                </TextBlock>

                <!-- Email -->
                <TextBlock Text="📧 İletişim" 
                           FontSize="16" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>
                <TextBlock FontSize="14" Margin="0,0,0,20">
                    <Hyperlink NavigateUri="mailto:onder7@gmail.com" 
                               RequestNavigate="Hyperlink_RequestNavigate"
                               Foreground="{StaticResource AccentBrush}">
                        onder7@gmail.com
                    </Hyperlink>
                </TextBlock>

                <!-- Features -->
                <TextBlock Text="✨ Özellikler" 
                           FontSize="16" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>
                <TextBlock Text="• Real-time TCP/UDP monitoring" 
                           FontSize="13"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Text="• Process management &amp; kill" 
                           FontSize="13"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Text="• Bandwidth charts &amp; statistics" 
                           FontSize="13"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Text="• History tracking &amp; database" 
                           FontSize="13"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Text="• TCP flag analysis" 
                           FontSize="13"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
        </Border>

        <!-- Close Button -->
        <Button Grid.Row="4" 
                Content="Kapat" 
                Click="CloseButton_Click"
                HorizontalAlignment="Center"
                Margin="0,20,0,0"
                Padding="30,10"
                Style="{StaticResource PrimaryButton}"/>
    </Grid>
</Window>
